xgboost>=2.0,<3
numpy>=1.26,<3
pandas>=2.1,<3
# On Digital Research Alliance / Compute Canada, PyArrow is provided via the `arrow` module
# (and may be newer than PyPI constraints). Keep this range broad enough to accept that.
pyarrow>=15,<23
pyyaml>=6.0.1,<7


